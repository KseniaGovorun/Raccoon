<%= turbo_frame_tag "new_tweet" do %>
  <%= simple_form_for @tweet do |f| %>
    <%= f.text_area :body %>
    <%= f.button :submit, class: "submit" %>
  <% end %>
 <%= link_to 'Cancel', tweets_path %>
<% end %>