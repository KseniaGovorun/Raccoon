<%= turbo_frame_tag @tweet do %>
  <%= simple_form_for @tweet, url: tweets_path(@tweet) do |f| %>
    <%= f.text_area :body, class: "w-full" %>
    <%= f.button :submit, class: "submit" %>
  <% end %>
  <%= link_to 'Cancel', tweets_path, class: "link" %>
<% end %>
