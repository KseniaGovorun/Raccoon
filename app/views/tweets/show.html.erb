<div class="container mx-auto p-4">
  <h1 class="title">Tweet</h1>
  <div class="space-y-6">
    <div class="box">
      <div class="relative">
        <span><%= @tweet.user.username %></span> 
        
        <div class="label2">
          <div class="ml-auto">
            <span class="text-timestamp"><%= @tweet.created_at %></span> 
          </div>
        </div>

        <p class="mb-2"><%= @tweet.body %></p>

        <div class="label2">
          <div class="flex items-center ml-auto">
            <% if current_user.id == @tweet.user_id %>
              <%= link_to "Edit", edit_tweet_path(@tweet), class: "edit-link" %>
              <%= link_to "Delete", tweet_path(@tweet), method: :delete, class: "delete-link" %>
            <% end %>
            <span class="ml-4">♡ Like</span>
            <span class="ml-4">💬 Comment</span>
          </div>
        </div>
      </div>
      <div>
      <%= link_to "Back", tweets_path, class: "link1" %> 
</div>
    </div>
  </div>
</div>
